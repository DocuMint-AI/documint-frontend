# Environment variables for DocuMint Backend
# Copy this file to .env and configure the paths to your credential files

# Required for AI features (file paths)
GEMINI_API_KEY_FILE=.cheetah/gemini-api-key.txt
GOOGLE_APPLICATION_CREDENTIALS=.cheetah/gcp-credentials.json

# Security settings (file path)
SECRET_KEY_FILE=.cheetah/documint-secret-key.txt

# Optional: Direct environment variables (alternative to file paths)
# GEMINI_API_KEY=your_gemini_api_key_here
# SECRET_KEY=your-secret-key-for-jwt

# CORS settings (if different from defaults)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000